Here we present the four isolated theropod teeth from the Guimarota fossil site. 
We add the log scaling of the variables (using R) and we use the trained models to classify the specimens (using Python).
